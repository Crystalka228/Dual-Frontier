# Save — Сохранение/загрузка мира

## Назначение
Сериализация и десериализация состояния `World` на диск. `ISaveSystem`
— контракт, `SaveSystem` — реализация, `SaveFormat` — версия формата и
заголовок файла (для миграций старых сохранений).

## Зависимости
- `DualFrontier.Core` — `World`, `ComponentStore`
- `DualFrontier.Contracts` — компонентные интерфейсы

## Что внутри
- `ISaveSystem.cs` — интерфейс `Save(path)` / `Load(path)`.
- `SaveSystem.cs` — реализация через бинарный формат (планируется).
- `SaveFormat.cs` — версия и заголовок файла, миграции.

## Правила
- `Save` / `Load` — **синхронные**. Асинхронность (прогресс-бар и т.п.)
  обеспечивает вышестоящий слой. Это явное правило THREADING.
- Не использует `using Godot` — пишем и читаем обычные файлы.
- Версия формата строго возрастает. Несовместимые сохранения — явная ошибка
  с миграционной подсказкой.

## Примеры использования
```csharp
ISaveSystem saves = new SaveSystem(world);
saves.Save("saves/slot1.dfsave");
// ...
saves.Load("saves/slot1.dfsave");
```

## TODO
- [ ] Фаза 1 — минимальный бинарный формат (компоненты + entities).
- [ ] Фаза 3 — миграции версий `SaveFormat`.
- [ ] Фаза 3 — сериализация мод-зависимых компонентов по id типа.
