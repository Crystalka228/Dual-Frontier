# Godot 4+ specific ignores
.godot/
.import/
/android/
export_presets.cfg
*.translation
*.tmp
*.uid
*.import

# .NET build output
bin/
obj/
*.lscache
*.user
.vs/

# CMake build artifacts (native core experiment)
native/DualFrontier.Core.Native/build/
native/DualFrontier.Core.Native/out/

# BenchmarkDotNet artifacts
BenchmarkDotNet.Artifacts/

# Stray native DLL copies next to projects (the .csproj <None Include> places
# the canonical copy under bin/ at build time; these are leftovers from
# manual post-build copies and must not be versioned)
DualFrontier.Core.Native.dll
tests/DualFrontier.Core.Benchmarks/DualFrontier.Core.Native.dll
*.zip

# Extracted Kenney asset packs and Cinzel font — local-only derived state.
# The source .zip files committed at repo root are the SoT; extractions are
# performed by the contributor locally and must not pollute git history.
/assets/cinzel/
/assets/Cinzel/
/assets/kenney_*/
/assets/Kenney_*/
