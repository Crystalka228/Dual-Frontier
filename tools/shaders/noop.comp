#version 450

// V0.B noop compute shader — smallest valid Vulkan compute pipeline.
// Used for compute pipeline registration + dispatch round-trip test (Commit 12).
layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

void main() {
    // Intentionally empty.
}
