<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>DualFrontier.Governance</RootNamespace>
    <AssemblyName>DualFrontier.Governance</AssemblyName>
    <IsPackable>false</IsPackable>
    <!--
      net10.0 per REGISTER_INVERSION brief 7.2 (project-native substrate; SDK
      10.0.204 + runtime 10.0.8 confirmed present at Phase 0). The rest of the
      solution targets net8.0; the installed SDK builds both TFMs in one build.
    -->
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="YamlDotNet" />
  </ItemGroup>

</Project>
