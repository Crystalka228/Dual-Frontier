<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>DualFrontier.Governance</RootNamespace>
    <AssemblyName>DualFrontier.Governance</AssemblyName>
    <IsPackable>false</IsPackable>
    <!--
      net10.0 per REGISTER_INVERSION brief 7.2 (project-native substrate; SDK
      10.0.204 + runtime 10.0.8 confirmed present at Phase 0). As of the
      STACK_UPDATE cascade the whole solution targets net10.0; this explicit
      override predates that bump and is retained as documentation.
    -->
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="YamlDotNet" />
  </ItemGroup>

</Project>
