---
register_id: DOC-E-DRAFTS_RATIFICATION_REVIEW_REPORT
project: Dual Frontier
category: E
tier: 3
lifecycle: EXECUTED
owner: Crystalka
version: 1.0
first_authored: 2026-07-17
last_modified: 2026-07-17
content_language: en
next_review_due: null
title: 'Drafts Ratification Review Report — Phase A of DRAFTS_RATIFICATION (D1: seven drafts re-verified at 48983c4 — code anchors EXACT wholesale, ~200 anchor verdicts; 41 fix ids + 2 mechanical classes; six READY-WITH-FIX, PSC retarget-only, zero NOT-READY; HALT-1 decision basis)'
review_cadence: on-status-transition
reviewer: Crystalka
---

# Drafts Ratification Review Report — Phase A of DRAFTS_RATIFICATION

**Generated by:** Claude Code (Fable 5), executor session, DRAFTS_RATIFICATION cascade Phase A
**Generated on:** 2026-07-17
**Brief:** `tools/briefs/DRAFTS_RATIFICATION_BRIEF.md` v1.0 (D1 deliverable)
**Repository HEAD:** `48983c4` on `claude/drafts-ratification` (= `main` `2309f9b` + the operator-ruled H3 exclusion rider); first cascade under register schema 2.0
**Discipline:** Phase A is read-only; this report is its only filesystem output. Wave R = four parallel read-only review agents (A1 EAM / A2 ELT+ROL+CMM / A3 TCM+IAC+PSC / A4 mechanical censuses); orchestrator composed and spot-verified. Every count carries its verbatim expression; quotes are transcribed, not paraphrased.

> **Purpose.** Claim-by-claim re-verification of the seven 2026-07-15 cross-cutting AUTHORED drafts against code and the post-rework corpus at current HEAD (EVT-R4 ratification-checklist item [6], widened per the previous D1's ORC-3), feeding the HALT-1 operator gate: per-draft ratification verdicts (six flips now; PSC held AUTHORED); the fix list (FIX-NOW / DEFER / REJECT per finding); both mechanical-class replacement forms; the optional F-39 rider decision. No Phase B edit outside the ratified list.

---

## R0 — Base state (Phase 0 record)

**(R) facts, re-verified.** `main` = `2309f9b` (clean apart from the working-tree caveats below); working branch `claude/drafts-ratification` at `48983c4`. Register DERIVED on schema 2.0: `REGISTER.yaml` header "DERIVED ARCHIVE … Never hand-edited", `schema_version: 2.0`, `register_version: 2.0`. `CURRENT_AUTHORITY_SURFACE.yaml` = **128 entries (104 Live + 24 LOCKED)** — verbatim expressions: `(Select-String -Path docs\governance\CURRENT_AUTHORITY_SURFACE.yaml -Pattern 'register_id:').Count` = 128; `… 'lifecycle: LOCKED'` = 24; `… 'lifecycle: Live'` = 104. All seven drafts present in `docs/architecture/` with their expected `DOC-A-*` register_ids, all Tier 1 AUTHORED. `AUDIT_TRAIL.yaml` head comment confirms "hand-edited source of truth (FRAMEWORK 14.6)"; entry shape captured from the last recorded EVT as the C7 append template.

**Baseline gates (regression anchor).**

| Gate | Result |
|---|---|
| `dotnet build DualFrontier.sln -c Release` | Build succeeded, **0 Warning(s), 0 Error(s)** (16.38 s) |
| Full-sln test run (TESTING_STRATEGY §8 harness: Start-Process + trx + watchdog, no pipe) | **10 suites, 1166 passed / 0 failed / 5 skipped** — exact (R)-fact match |
| `dotnet run --project tools/DualFrontier.Governance -- validate --armed` | **exit 0** after the H3 rider (see below): 338 parsed / 338 in-scope / errors 0 / gate findings 0; G-RATIO monitor 151/338 (44.7%) report-only — out of scope per brief §15 |

Per-suite, from the TRX-backed run: ManifestRewriter 7/7, Persistence 6/6, Core.Interop 202/202, Systems 2/2, Application 45/45, **Governance 64/64**, Analyzers 54/54, Runtime 293/293, Modding 400/400, Core.Tests 93 passed / 5 skipped / 98. The 5 skips are the `SchedulerExtremeTests` Skip-guarded quarantine (S2 under F-30; S3/S4/S5a/S5b under F-31). The F-40 cross-suite flake (`UnloadModNativeState_VacuousUnload_Succeeds`) did **not** recur — Modding 400/400.

**H3 event and operator ruling (recorded deviation, pre-C1).** The first `validate --armed` run returned exit 1 with exactly two G-PATH errors: `docs/methodology/BRIEF_TEMPLATE.md` and `docs/methodology/RECON_KICKOFF_TEMPLATE.md` — operator-authored architect scaffolds copied onto disk 2026-07-17 ~06:27 (after brief authoring and after session start), untracked, no frontmatter, in governed scope: the fail-closed G-PATH gate (FRAMEWORK 14.2) fired correctly on live-tree drift, not on a brief error. H3 raised in chat; the operator ruled **exclusion-rider**: two exact-file entries with rationale in `tools/governance/SCOPE_EXCLUSIONS.yaml` (CD2 precedent form; no glob widening). Rider commit `48983c4`; `validate --armed` re-run exit 0. Mechanical note: the rider commit was amended once to strip a UTF-8 BOM that PowerShell 5.1 `Set-Content -Encoding utf8` injected into the subject line (pre-push local amend; message content unchanged).

**Working-tree caveats (recorded, untouched).** `.claude/scheduled_tasks.lock` modified — scheduler session-lock noise (sessionId/pid/acquiredAt rewritten by local tooling); never staged; all cascade commits are path-scoped. Untracked: `tools/briefs/DRAFTS_RATIFICATION_BRIEF.md` (enrolls at C1) and the two excluded operator templates (operator's files, not the cascade's to commit).

**Census baselines (pre-measure, census-delta law; canonical `--count-matches` forms).**

| Census | Expression | Baseline |
|---|---|---|
| DFK-WAIVER HARD pin | `rg -n "DFK-WAIVER" src/` | **2** (both `ValidationLayer.cs:90,137`) — pin holds |
| Class 1, brief's exact token | `rg -l "normative-current-candidate" docs/architecture/` | **12 files** (brief §2 predicted 14 — see nuance below) |
| Class 1, class marker | `rg --count-matches "Ratification checklist" docs/architecture/*.md` | **14 files × 1** — the banner CLASS is 14/14 |
| Class 2 qualifier | `rg --count-matches "AUTHORED draft" docs/architecture/*.md` | **87 matches across 11 files** (auxiliary line-count form `rg -n … \| wc -l` = 80 — some lines carry two qualifiers; both recorded per Lesson #N18 method discipline) |

Class-1 nuance (brief-vs-live: the code is true, recorded per the brief's own rule): the exact token `normative-current-candidate` appears in 12 of the 14 successors' Role rows; MODDING (Role = guide, non-normative) and FHE_INTEGRATION_CONTRACT carry the banner + checklist in variant wording without that token. The class is enumerated by ALL marker forms in R4; the closure gate is 0 across all forms.

Class-2 per-file baseline (`--count-matches`): VULKAN_SUBSTRATE 14, MOD_OS_ARCHITECTURE 12, EVENT_BUS 12, KERNEL_ARCHITECTURE 12, SCHEDULER_ARCHITECTURE 9, ECS 8, THREADING 7, FIELDS 6, ARCHITECTURE 3, FHE_INTEGRATION_CONTRACT 3, CONTRACTS 1.

**Mandatory reads (executed in full):** the brief; `docs/reports/CORPUS_REWORK_REVIEW_REPORT.md` R4 + R5 + ORC rows (R4-7..R4-13 seeds carried verbatim into R1–R3 below); the seven drafts in full; FRAMEWORK §14 (14.1–14.9); METHODOLOGY §12.7 + §12.9. Frontmatter templates captured (Lesson #N14, 2.0 edition): LOCKED successor shape from `KERNEL_ARCHITECTURE.md` + `MODDING.md` (post-flip `special_case_rationale` ratified-lineage precedent), AUTHORED draft shape from all seven, E/3/EXECUTED report shape from the previous D1, AUDIT_TRAIL head comment + last full EVT entry (with `lifecycle_transitions` list form) as the C7 append template. Flip-field convention confirmed from the CORPUS_CLOSURE ratification EVT: `AUTHORED → LOCKED, version → 1.0.0, review_cadence → on-change+annual, next_review_due → '2027-Q3', AUTHORED-override rationales retired to ratified-lineage form`.

---

## R1 — EAM (A1 agent: EXECUTION_AUTHORITY_MATRIX)

**Headline.** Every code anchor (32 sites across src/native/tests) verifies **EXACT** — code unchanged since `6f39903` (one cosmetic drift: S10 probe `SchedulerExtremeTests.cs:1007` → `:1009-1010`). Every doc anchor into the 14 replaced-in-place successors is DRIFTED (line renumber; current locations enumerated). Three of five §6.1 "conflicts with LOCKED law" were **resolved in-corpus by the KERNEL rework**; conflict 4's doc (KFNS) retired to `historical/`; only conflict 3 (PA-003 §3.4 gated-tenure refinement) still stands. **All 8 cutover gates ratify TRUE (OPEN as written).** No §2 matrix row is factually wrong, but the R9/R11/R12 vacant-row wording ("ownerless / currently unwritten") is stale — the sibling AUTHORED drafts (ELT+ROL / IAC / PSC) now occupy those rows; re-word to "no ratified (LOCKED) owner; AUTHORED draft <name> pending".

### R1.1 Gate re-verification (§3) — the ratification core

| Gate | "Open while" predicate | Verdict | Evidence at `48983c4` |
|---|---|---|---|
| GATE-S1 | any prod `RegisterSystem` passes `ReadOnlySpan<uint>.Empty` | **OPEN** | `GameBootstrap.cs:173-174` verbatim |
| GATE-S2 | no `tests/` compares the planes' phase partitions on the prod set | **OPEN** | only native exercise = `selftest.cpp:1034` synthetics; sole equivalence suite is GPU `V1DiffusionEquivalenceTests` |
| GATE-S3 | `SchedulerAdapter` zero prod call sites | **OPEN** | THREADING §3:73 "only callers today are BatchedCallbackTests.cs"; ARCHITECTURE §3:97 "zero production call sites, verified" |
| GATE-S4 | every prod wake sub is `SubscribeTimer(i,1)` | **OPEN** | `GameBootstrap.cs:179` sole prod wake sub |
| GATE-B1 | `BusFacade` zero prod ctor sites | **OPEN** | repo grep `new BusFacade(` → 0 in src/ (sole hit = the S10 test) |
| GATE-B2 | no flag-on/off lockstep harness | **OPEN** | native path exercised only by stress/extreme suites |
| GATE-B3 | production registers zero native subscribers | **OPEN** | EVENT_BUS §4:90 "vacuously today" |
| GATE-B4 | `DomainEventBus` exposes no counters | **OPEN** | EVENT_BUS §7:114 "keeps no counters of any kind" |

§2 matrix de-facto column: R1–R8, R10, R13 HOLD (spot-verified per row); R9/R11/R12 hold on substance with the stale-wording fix above. §5 observability table: **every named enforcer real on disk; zero false enforcement verbs** (defects are citation-address only — DFK003/003_1 live in ANALYZER_RULES §1.1 not "§4"; the DFK012/015/018 deferrals live in §4:162-164). The §5 pattern claim (split rows = deferred rules) CONFIRMED.

### R1.2 Seeded finding R4-13 — resolved

EAM:186/187/190/200 ANALYZER_RULES cites → `ANALYZER_RULES.md §1.1 (shipped rule registry)` for DFK003/DFK003_1/DFK011/DFK019_A; `ANALYZER_RULES.md §4 (deferred registry)` for DFK012/DFK015/DFK018/DFK020 (successor is 219 lines; nothing near "line 271"). Truth-pins: §2 column header gains "; re-verified 2026-07-17 at HEAD `48983c4`, DRAFTS_RATIFICATION"; baseline line (:33) and closing line (:223) gain the re-verification clauses (exact texts in R5.1 / EAM-2).

### R1.3 Retarget list (Phase B/C2 source; §6.1 form — bare line-pins forbidden in LOCKED prose)

~40 bare `Doc.md line N` anchors convert wholesale to `DOC §n (topic)` form — the consolidated mapping table (KERNEL Part 0 К-L rows; THREADING §3/§6; EVENT_BUS §2/§4/§6/§7; MOD_OS §0/§2.2/§2.3; ARCHITECTURE §6; FRAMEWORK §14.7; PROJECT_AXIOMS §3.4; K_CLOSURE_REPORT §1.2/§2.17; S10 `:1009-1010`) is carried verbatim in R5.1 (EAM-3) and applies as-listed. Notable dispositions:
- **EVENT_BUS "schema v1"** (EAM:95/215): claim is CODE-TRUE — `background_queue.cpp:209` "Header bytes per Item 31 wire format (К10.2 schema v1)" + `df_background_queue_serialize/deserialize` (:242/:275); orchestrator-verified. Retarget cites the code + `EVENT_BUS.md §6 (Background-tier capacity)` + PSC (save-boundary owner) instead of the retired predecessor line.
- **P2 worked example** (EAM:175): the ARCHITECTURE-vs-MOD_OS "standoff" is largely pre-resolved (successor §1.4:102 concedes shared-mod references); annotate "(pre-rework example; the umbrella since clarified §1.4)" or re-anchor.
- **§6.1 conflicts disposition**: 1, 2, 5 → resolved-notes (KERNEL §4 Rule 5 + Annex A-1 sanctions batched callbacks; §6:577 retires "pool idle"; К-L4:138 scopes FNV-1a prohibition to component ids, names event-id space non-violating); 4 → resolved-by-retirement note (KFNS SUPERSEDED → historical/, successor SCHEDULER_ARCHITECTURE; no historical/ edits); 3 → KEEP (PA-003 §3.4 refinement still owed, PROJECT_AXIOMS §5 protocol).
- R9/R11/R12 vacant-row rewording (three rows, one sentence each).

### R1.4 Verdict — **READY-WITH-FIX**

Substance ratifies TRUE: gates OPEN as written, matrix true, enforcement claims real, and the LOCKED corpus already pins to this doc (THREADING §3, KERNEL К-L12/К-L15, ARCHITECTURE §3, EVENT_BUS §4 all cite "EXECUTION_AUTHORITY_MATRIX.md §3 (AUTHORED draft)" as the cutover-gate authority — "no ratified deletion trigger exists"). Fixes are citation-integrity only: (1) wholesale §6.1-form re-homing; (2) conflict-inventory refresh (3 of 5 resolved); (3) vacant-row rewording; (4) P2 example annotation; (5) preamble.

### R1.5 Preamble replacement (drafted for HALT-1)

Full text in R5.1 (EAM-9). Shape: LOCKED-law banner with EVT id; "Status of the cutover claims" re-verification paragraph; conflict rule in force (honest §6.1 inventory, no precedence grant); **"Deferred fold path (forward amendment queue — NOT executed by this cascade)"** table replacing the old fold-table (§2→ARCHITECTURE MINOR; §3→KERNEL Part 0 annotations; §4→FRAMEWORK §14.7 schema-amendment; §1/§5/§6 standalone) with the explicit note that **§4 P1–P3 remain ADVISORY until the §14.7 amendment lands** (preserving EAM §4's own honesty). Recommendation: keep the fold table as forward queue + register the three destinations as ROADMAP amendment items.

### R1.6 Defer-candidates (F-ledger shapes)

S10 anchor harmonization (cosmetic); version-0 idiom sites (`GameBootstrap.cs:241`, `EntityEncoder.cs:85`) — link to IAC §2's DFK-rule work order so the fix lands once; sibling-draft cross-links for R9/R11/R12 rows; KFNS live-reference retargets (SCHEDULER_ARCHITECTURE/THREADING §2). Orchestrator spot-checks CONFIRMED: `src/DualFrontier.Persistence/Snapshots/` exists (5 record types — R12 phrasing holds); "schema v1" code-true (above).

## R2 — Lifecycle-law cluster (A2 agent: ELT + ROL + CMM)

### R2.0 Cross-cutting findings (P1–P5)

- **P1 — historical-doc cites lack the `historical/` prefix AND run ~+8 lines stale.** ISOLATION, MOD_PIPELINE, KFNS, OWNERSHIP_TRANSITION, MIGRATION_PLAN relocated to `docs/architecture/historical/` at the rework; the added ~8 frontmatter lines shift every body-line cite by a constant. Verified offsets: ISOLATION `:70→:78`, `:65-73→:73-82`; MOD_PIPELINE `:319→:327`, `:33→:41`, `:228→:236`, `:339→:347`, `:340→:348`; OWNERSHIP_TRANSITION `:43→:51`. Quoted content accurate — only path + index stale.
- **P2 — live-successor cites drifted** (docs replaced in place); a subset WRONG (content retired/corrected) — see P4.
- **P3 — code cites overwhelmingly EXACT** at `48983c4`; every honestly-marked code-truth gap re-verified TRUE (ExecutePhase no-catch `:149-164`; `UnloadAll` zero production callers `:780`; `InvokeDeferred` no-catch; `NativeWorld` finalizer-only destroy; `df_bus_clear` test-only `bus_native.h:155`; `SchedulerAdapter.ClearCallback` test-only).
- **P4 — META-FINDING (governs law-readiness): the corpus rework already RESOLVED most of the drafts' LOCKED-conflict inventories, frequently CITING these drafts as the resolution source.** Confirmed: MOD_OS §9.1 (lines 513-518) adopts the ELT §1 commit/reclaim split *"per ENGINE_LIFECYCLE_AND_TRANSACTIONS.md (AUTHORED draft) §1"*; MOD_OS §9.5:571 resolves the three-way fault-timing (was N-8); MOD_OS §9.4:559 canonicalizes one unload chain (was N-9); KERNEL_ARCHITECTURE:577 retires the §1.4 "pool idle / single-threaded writes" stratum; FIELDS:147 corrects "non-blocking" → sync-blocks; THREADING §7 (122-130) is now the normative home of the cycle/snapshot rule with FEEDBACK_LOOPS §3 refusing to restate it. **The LOCKED→AUTHORED forward-citation is a governance inversion that ratification closes.**
- **P5 — §6.1 citation-form violation is universal**: every line-number doc cite in the three drafts is the fragility class CODING_STANDARDS §6.1 outlaws; independent of drift, doc cites reform to `DOC §n (topic)` / bare К-L ids. The single largest mechanical retarget category.
- **R4-13 uniform re-verification clause** (recommended for all drafts): *"Authored 2026-07-15 against HEAD `6f39903` (historical authoring fact). Code-truth claims re-verified at the DRAFTS_RATIFICATION cascade, HEAD `48983c4`, 2026-07-17 (EVT-2026-07-17-DRAFTS_RATIFICATION); src/ and native/ unchanged in substance since authoring — all code anchors re-confirmed. Doc anchors authored against the pre-rework corpus are retargeted per §6.1 in this cascade."*

### R2.1 ELT — verdict **READY-WITH-FIX**

Anchor highlights (full agent tables held in the Phase-A working record; WRONG/must-fix rows): E12/R4-11 `OWNERSHIP_TRANSITION.md:43` → `historical/OWNERSHIP_TRANSITION.md:51` (§"Transition table" closing rule; doc superseded by DOC-J-GOLEM_OWNERSHIP); E13/E14 MOD_OS `:861/:946/:944` — sentences retired: the §9-vs-§9.5.1 contradiction is RESOLVED at MOD_OS §9.1 (ELT's own law absorbed); E17 quiescence is MOD_OS **§9.6** (draft says §9.7 — now "Hot-reload-disabled mods"); E9/E10/E11 ISOLATION/KFNS/MIGRATION_PLAN cites need `historical/` prefix + offsets; all code cites EXACT. Truth-law audit: all defect claims TRUE at HEAD; zero honesty slips. Key retargets (exact §6.1-form texts carried in R5): §1.1/§6-1 → MOD_OS §9.1 resolved-note; §2.3/§6-2 → MOD_OS §9.5 + detection-gap note ("MOD_OS §9.5 fences to this draft"); §6 conflicts 1/2/6 re-marked RESOLVED, 3/4/5 stay open re-anchored. Preamble replacement drafted (R5): ratified-in-force; §1 law already absorbed by MOD_OS; forward queue = §3 state machines → MOD_OS §9.1, §2.5 swapchain → VULKAN, §2.6 shutdown + §4 taxonomy → MOD_OS §10-family or standalone shutdown-law doc.

### R2.2 ROL — verdict **READY-WITH-FIX**

G1–G6 shutdown-gap inventory re-verified **fully TRUE** at HEAD (world never destroyed; no native scheduler/bus teardown; UnloadAll zero callers; gameContext undisposed; no stated order). Must-fix WRONG cite R-e: `VULKAN_SUBSTRATE.md:1317` "waitIdle … only for save snapshots and shutdown" — the LOCKED successor **retired that phrasing at VULKAN:604** (no save path exists; recreation also uses it); R-f: MOD_OS §9.7 → §9.6. §7.1-1 re-marked RESOLVED (MOD_OS §9.1 adopted the logical/physical split; session finding C7 closed); §7.1 conflicts 2–5 genuinely open (missing-invariant/ABI class). All code cites EXACT; zero honesty slips. Preamble replacement drafted (R5).

### R2.3 CMM — verdict **READY-WITH-FIX** (carries the wave's one honesty slip)

- **R4-9 CONFIRMED — the honesty slip:** CMM §8 (line 241) "the wired analyzer registers 17 rule stubs, all non-detecting — zero diagnostics fire" is **false at HEAD**: the analyzer went detecting+enforcing at А'.9.1 Phase β/γ (2026-07-01, before the draft's authoring); THREADING:95 is now a code sample and THREADING §8 (137-138) carries the corrected truth. Replacement text drafted (R5): live analyzer, 17 detecting rules at Release-1.0 severities, waiver pin = 2, residual gap = none of them covers async-affinity/span-escape/missing-Dispose (a coverage gap, not a stub state).
- **R4-8 CONFIRMED:** cycle/snapshot rule re-homed — §4 edge 4 + §9.4 re-attribute to THREADING §7 (normative), FEEDBACK_LOOPS demoted to gameplay-application cite; mechanism still unbuilt (TRUE, THREADING §7 says "Not implemented" and cites this draft §9 item 4 back).
- **R4-12 CONFIRMED + root-caused:** version bumped at each rework link-retarget commit (`fe27fbe` R1, `6888246` R2, `0145f1b` R4) but the in-doc change history never followed. Proposed reconciliation (architect ruling at HALT-1): add 0.1.1/0.1.2/0.1.3 PATCH rows + bump frontmatter 0.1.2 → **0.1.3** (matches the register narrative); alternative = fold R4 into the 0.1.2 row and correct the narrative.
- §9.1 (KERNEL §1.4) and §9.3 (FIELDS "non-blocking") conflicts re-marked **RESOLVED** (successors retired/corrected the texts). C5/C6 WRONG cites (`VULKAN:1317`, `FIELDS:372`) retarget to corrected sections; body KFNS cites gain `historical/`. All code cites EXACT; §7 deferred-catch asymmetry re-verified TRUE (the stated top priority stands).

### R2.4 Cluster defer-candidates (→ ROADMAP engineering queue)

The **shutdown-law implementation family** — referenced by the LOCKED corpus, absent from ROADMAP as a named item: ExecutePhase per-system catch + SystemOrigin dispatch; UnloadAll first production caller; world-shutdown transaction (Join result checked, deterministic NativeWorld.Dispose, teardown order); `df_world_active_span_count` export + checked destroy ABI; `EngineSession : IDisposable` adoption sketch; deferred-handler catch symmetry + regression test; swapchain prepare-before-reclaim + failure-injection test; device-lost handler decision; TLA+ Item 18 kickoff; bounded-queue law owner; production native teardown surface (`df_bus_clear` promotion).

## R3 — Time-identity cluster (A3 agent: TCM + IAC + PSC)

**Cluster headline.** Code cites EXACT wholesale (≈45 anchors, incl. the full IAC version-0 site list — every listed line verbatim; `capi.cpp` catch-site recount = **72 exactly**, and the KERNEL successor §5 independently states "72 sites"). Doc cites systematically DRIFTED (verified EXACT-at-authoring for sampled anchors — pure rewrite-drift, not authoring error), with a WRONG subset where the successor REMOVED or CORRECTED content: `FIELDS:372` "non-blocking" (corrected at FIELDS §10:147), `FEEDBACK_LOOPS:40` "Phase 5" + `COMBO:39/:104` "Phase 4" (phase numbering retired), `EVENT_BUS:59` "schema v1" (phrase removed — but CODE-TRUE at `background_queue.cpp:209`, orchestrator-verified in R1.3), `KERNEL:391` + `ECS:81/:84` version-0 teaching (the C10/N19 fix landed — the successors now teach the CORRECTED form), `VULKAN §3.4` uint32 field_id sketch (corrected to string-id at §4.3). **Same meta-finding as R1/R2: the LOCKED corpus forward-references these drafts** — VULKAN §7.2 cedes determinism-class vocabulary to TCM; COMBO/FEEDBACK cite "aspirational D2, effective D0" per TCM §5; KERNEL §5 forward-refs IAC §3–§4; ECS §2/§5 defers the IsValid fix to IAC §2; VULKAN §7.1, MOD_OS (714), FIELDS §4/§12 forward-reference PSC.

### R3.1 TCM — verdict **READY-WITH-FIX**

- **R4-7 resolved concretely:** the three dangling-phase cites (§1:50 THREADING:141 five-phase/v2.0.0; §1/§1.1/§7.5 FEEDBACK_LOOPS:40 "Phase 5"; §1.1/§7.5 COMBO "Phase 4") retarget to THREADING §2/§7 (tick-boundary anchor; the successor is v1.0.0 — the v2.0.0 change-row lives in `historical/THREADING.md`), FEEDBACK_LOOPS §2 (snapshot-pattern shape), COMBO §2 footnote (`[BridgeImplementation(Phase=5)]` = roadmap tag, not a scheduler PhaseId). §7.5 is **substantially closed by the rework** — re-marked.
- **R4-10 confirmed, worse than seeded:** FOUR fixed-seed production `Random` sites — `MovementSystem.cs:35` (**target-typed `new(42)`** — a `new Random(` grep misses it), `GameBootstrap.cs:96` (`ObstacleSeed` = 42 at :62), `RandomPawnFactory.cs:58`, `ItemFactory.cs:39` (injected seeds). Replacement texts for §4.3/§5-D0/§6 drafted (R5.3); the §6 no-own-RNG argument is STRENGTHENED; **new law nuance: the proposed analyzer ban must be semantic (type-based), not textual** — regex would miss the target-typed site. PSC §2's RNG row (`GameBootstrap.cs:96`) verified EXACT — PSC already carries the accurate version; TCM aligns to it.
- **§7 inventory re-scored:** §7.1 FIELDS "non-blocking" **RESOLVED by rework** (FIELDS §10 carries TCM's exact proposed amendment — mark DONE in the fold queue); §7.5 **RESOLVED**; §5.2/§7.2 COMBO-vs-VULKAN **SOFTENED** — both LOCKED texts now defer to TCM, so it is a decision TCM owns (Option A/B), no longer a LOCKED-vs-LOCKED contradiction; §7.3 input-path-absent STILL TRUE; §7.4 К-L16-drain-vs-Item-34 STILL LIVE with reconciliation now **delegated to PSC §1 (PS-2) by VULKAN §7.1 itself**.
- Truth-law: input-discard, MaxAccumulator≤5, no-RNG-service, quiescence predicate — all TRUE; the single WRONG truth-claim is the RNG census (fixed above).

### R3.2 IAC — verdict **READY-WITH-FIX**

- **Version-0 census corrected:** the enumerated site list is EXACT line-by-line, but the aggregate is imprecise — on-disk truth = **nine** production systems (matches the ratified ECS §5 "9; InventorySystem constructs none"), ≈20 sites: the draft's list omits `ComfortAuraSystem.cs:61,:69` and `HaulSystem.cs:110,:144`; sibling fabrication `WriteBatch.cs:221` (Version=1) uncited (ECS §5 mentions it). Replacement census text drafted (R5.3).
- **§7 inventory re-scored:** item 1 (VULKAN uint32 sketch) **RESOLVED** (successor §4.3 string-id); item 2 (ECS:81/KERNEL:391 teach version-0 + ECS:84 "Access is permitted") **RESOLVED** — the successors teach the corrected form and defer the identity fix to IAC §2; the "teaching texts instruct the defect" claims in §0/§2 are now STALE (the CODE defect persists; the DOC teaching does not — replacement texts drafted); item 3 (IsValid vs is_alive) STILL LIVE (`EntityId.cs:38` + `entity_id.h:14-16` + `world.cpp:75` unchanged) — IAC owns; item 4 (EntityEncoder) STILL LIVE → PSC PS-5; item 5 (capability grammar) PARTIAL — FIELDS §8 unified the field-grammar half to `<provider>.field.<verb>:<field-id>`; item 6 ACCOMMODATED (KERNEL §5:573 freezes 1/0 and forward-refs IAC §3–§4).
- **"device-lost zero corpus hits" now literally false** (VULKAN carries OQ-V1/§6.3); substantively the mapping is still unspecified — text softened (R5.3).
- Truth-law: 72 catch-sites recount EXACT; no-CI TRUE; fabrication family TRUE. `df_capi.h` header line-pins sampled EXACT (orchestrator: `:267-271` serialize-content-not-ids verbatim); full header line-audit folds into the C4 apply pass.

### R3.3 PSC — verdict **RETARGET-NEEDED** (held AUTHORED; no ratification)

- One WRONG cite: `EVENT_BUS:59` "versioned wire format, schema v1" — phrase removed from the successor; retarget to KFNS Item 31 (`historical/`, `df_scheduler_serialize_background_queue`) + `EVENT_BUS §6 (background-tier capacity & save-format)`; the claim itself is code-true (`background_queue.cpp:209` "Item 31 wire format (К10.2 schema v1)").
- Remaining doc cites DRIFTED with known homes: MOD_OS §12-D-6/§9.4 → **§9.8** (590-594; forward-refs PSC at 714); FIELDS §Save/load → **§12** (FENCED, defers to PSC) + §4 forward-cites **PS-7**; VULKAN §7.2.0/§7.2.1/§7.3 → §7.1 (forward-refs PSC §1)/§7.2 (cedes classes to TCM)/§5.4; FHE D7 → §D7 (~104-108, defers-to rows 40/178); K_L3_1_AMENDMENT_PLAN:91 → ~:87/:101-103 (K-L3 table / Q4.b region). Code cites all EXACT (orchestrator re-verified README:60-62/:75-77 verbatim).
- **PS-2 is now load-bearing for the LOCKED corpus** (VULKAN §7.1 delegates the drain-vs-snapshot reconciliation to PSC §1) — validates the hold-AUTHORED-but-retarget posture.
- Refreshed verification note drafted (R5.3): banner intact; anchors re-verified/retargeted note with EVT id; frontmatter `next_review_due` → `'post-persistence-milestone closure'`.

### R3.4 Cluster defer-candidates (→ F-ledger / ROADMAP)

RNG-service gap (4 sites; blocks D1; semantic analyzer rule) — shared TCM/PSC; version-0 fabrication family (≈20 sites / 9 systems + bootstrap + encoder + Version=1 siblings; `df_world_acquire_versions` ABI + DFK-entity-identity rule + ABA tests; atomic migration per IAC §7-10) — shared IAC/EAM; IsValid-native-mismatch; EntityEncoder waiver-until-PS-5; ABI-negotiation absent (`df_abi_version`; no CI, native binary uncommitted); thread-affinity undeclared (§3.6); persistence-layer orphan family; no-config-contract; input-bridge absent; native-Normal-tier dormant.

## R4 — Mechanical censuses (A4 agent)

**Scope confirmation.** 14 LOCKED successors (frontmatter already `LOCKED / 1.0.0`; bodies carry the candidate boilerplate — the target). Predecessors verified in `historical/`: 13 direct + ANALYZER_RULES' three (ANALYZER_RULES, ARCHITECTURE_TYPE_SYSTEM, MAXIMUM_ENGINEERING_REFACTOR_TRACK_B_ACTIVATION) + MOD_OS' three (MOD_OS_ARCHITECTURE, MOD_PIPELINE, ISOLATION). ⚠ **SCHEDULER_ARCHITECTURE's predecessor is `historical/KERNEL_FULL_NATIVE_SCHEDULER.md`** — there is NO `historical/SCHEDULER_ARCHITECTURE.md` (banner adapted accordingly).

**Verbatim baseline expressions + counts.**

```
rg -l  "normative-current-candidate" docs/architecture/*.md → 12 files (token); the CLASS is 14
rg -c  "Ratification checklist"      docs/architecture/*.md → 14 files × 1
rg -c  "current-truth candidate"     docs/architecture/*.md → 14 files × 1 (banner marker a)
rg -c  "authored-rework"             docs/architecture/*.md → 14 banners + ANALYZER_RULES:219 changelog cell
rg -n  "pending ratification"        docs/architecture/*.md → 4 hits / 2 files: ARCHITECTURE(151,158), CONTRACTS(152,159)
rg -n  "upon Crystalka ratification" docs/architecture/*.md → 14 successor banners + 7 draft banners
rg -n  "AUTHORED draft"              docs/architecture/*.md → 80 lines / 11 files (87 matches --count-matches)
rg -n  "advisory until ratified"     docs/architecture/*.md → 3: THREADING(75), SCHEDULER(199,264)
rg -n  "\(draft, this session\)"     docs/architecture/*.md → 5: PSC(96,138,140,177,178)
```

Expected after-state: **class 1 → 0 across all marker forms**; **class 2 → 17 lines keep the qualifier, all referencing PSC**; 63 lines drop it.

### R4.1 Census 1 — per-file table (line numbers at `48983c4`)

| # | file | banner | checklist | Role row (current) | pending-amendment (d) | other (e) |
|---|---|---|---|---|---|---|
| 1 | ANALYZER_RULES | 30 | 31 | 37 `normative-current-candidate` | — | 219 `1.0.0-draft` changelog cell (+218 note) |
| 2 | ARCHITECTURE | 28 | 29 | 33 `normative-current-candidate` | **151** `Tier 1, AUTHORED pending ratification.` | 158 `0.1.0 (AUTHORED, pending ratification)` |
| 3 | CONTRACTS | 28 | 29 | 33 `normative-current-candidate` | **152** same | 159 `0.1.0 (AUTHORED, pending ratification)` |
| 4 | ECS | 28 | 29 | 35 `normative-current-candidate` | — | 179 `0.1.0 (unreleased, AUTHORED)` |
| 5 | EVENT_BUS | 28 | 29 (extra clause re CMM/TCM §5–§7 cites) | 35 `normative-current-candidate` | — | — |
| 6 | FHE_INTEGRATION_CONTRACT | 28 ⚠ | 29 | 35 `dormant-contract` (NO candidate token) | — | — |
| 7 | FIELDS | 28 | 29 (extra clause re VULKAN ratified) | 35 `normative-current-candidate` | — | — |
| 8 | KERNEL_ARCHITECTURE | 50 | 51 (extra clauses: Annex A, REQ transfer) | 57 `normative-current-candidate` | — | — |
| 9 | MODDING | 28 | 29 | 35 `guide — non-normative` (NO candidate token) | — | — |
| 10 | MOD_OS_ARCHITECTURE | 30 (merge rationale) | 31 (extra clauses) | 37 `normative-current-candidate` | — | 727 `(candidate)` changelog cell |
| 11 | PERFORMANCE | 28 | 29 | 35 `normative-current-candidate.` + budget prose | — | — |
| 12 | SCHEDULER_ARCHITECTURE | 28 | 29 | 37 `normative-current-candidate` | — | — |
| 13 | THREADING | 28 | 29 | 35 `normative-current-candidate` | — | 193 `0.1.0 (unreleased, AUTHORED)` |
| 14 | VULKAN_SUBSTRATE | 28 | 29 | 33 `normative-current-candidate` | — | — |

### R4.2 Census 1 — replacement forms

- **(b) checklist line → DELETE** (all 14; the per-doc extra clauses were pre-ratification gating items, now satisfied — they die with the line).
- **(c) Role row** → `| Role | normative (ratified successor) |` (11 plain sites; MOD_OS/VULKAN keep the bold `**Role**` label; PERFORMANCE keeps its budget prose after the token swap: `**normative (ratified successor).** Budget numbers below are …`). **FHE:35 and MODDING:35 → NO CHANGE** (no candidate token; guide/dormant semantics preserved).
- **(d) amendment sentence ×2** → `Tier 1, LOCKED — amendments via FRAMEWORK.md §7.2 protocol.` (rest of each sentence unchanged; ARCHITECTURE:151 + CONTRACTS:152).
- **(a) banner → one-line ratified-successor note.** Template (per doc, predecessor path/id adapted): `> **Ratified successor.** LOCKED v1.0.0 per EVT-2026-07-17-CORPUS_CLOSURE_RATIFICATION (2026-07-17); predecessor docs/architecture/historical/<name>.md (DOC-A-<id>) is SUPERSEDED. Produced by the corpus rework of 2026-07-15 (session report: [ARCHITECTURE_DECOMPOSITION_CONTRACTS_SESSION_20260715](../reports/ARCHITECTURE_DECOMPOSITION_CONTRACTS_SESSION_20260715.md)); content verified against code at HEAD 35364c2.` The provenance sentence survives; only the candidate/ratification-pending machinery is removed. **Three flagged sites needing operator wording confirmation:** ANALYZER_RULES (three predecessors + the DF_TS §5 retirement clause survives), MOD_OS_ARCHITECTURE (three-predecessor merge rationale survives), FHE_INTEGRATION_CONTRACT (banner's "predecessor prevails until ratification" was already false for a doc with prior-LOCK history v1.0 2026-05-06 — bespoke wording reconciling prior LOCK with the 2026-07-17 event).
- **(e) changelog status cells — operator decision** (authored-session history vs alignment): ARCHITECTURE:158, CONTRACTS:159, ECS:179, THREADING:193, MOD_OS:727, ANALYZER_RULES:219(+218). Ratification convention adds a `1.0.0 | 2026-07-17 | Ratified LOCKED per EVT-…` row either way.

### R4.3 Census 2a — per-site actions (80 lines / 11 files; 63 drop, 17 keep)

Rule: references to the six ratifying drafts drop `(AUTHORED draft)` (and co-located `— advisory until ratified`); references to PSC KEEP it. Four **mixed** lines carry qualifiers for both classes and are edited per-token: **ECS:38** (drop IAC, keep PSC), **EVENT_BUS:38** and **EVENT_BUS:40** (drop CMM+TCM, keep PSC), **KERNEL_ARCHITECTURE:525** (drop IAC, keep PSC).

| File | Drop sites | Keep sites (PSC) |
|---|---|---|
| ARCHITECTURE | 38, 100, 146 (all EAM) | — |
| CONTRACTS | 148 (EAM) | — |
| ECS | 38*(IAC half), 40, 65, 122, 154, 167 (IAC) | 38*(PSC half), 168 |
| EVENT_BUS | 38*, 40* (CMM+TCM halves), 92 (EAM), 100, 110, 151 (CMM), 152 (TCM) | 38*, 40* (PSC halves), 155 |
| FHE_INTEGRATION_CONTRACT | 40, 74, 180 (all TCM) | — |
| FIELDS | 191 (EAM) | 38, 40, 80, 159, 187 |
| KERNEL_ARCHITECTURE | 260, 315, 588, 589, 609 (EAM); 485, 610 (ROL); 512, 525*, 573, 608 (IAC) | 525* (PSC half) |
| MOD_OS_ARCHITECTURE | 42, 482, 507, 515, 522, 573, 705 (ELT); 581, 713 (EAM) | 40, 595, 714 |
| SCHEDULER_ARCHITECTURE | 42, 177, 183, 199†, 210, 249, 264† (EAM); 222, 265 (ELT) | — |
| THREADING | 40 (CMM+ROL), 75† (EAM), 130, 170 (CMM+ROL), 181 | — (+193 changelog prose — operator decision) |
| VULKAN_SUBSTRATE | 316, 604, 630, 701 (ELT); 356, 703 (CMM); 472, 705 (ROL); 658, 704 (TCM); 706 (EAM) | 654, 660, 702 |

† = also delete the co-located `— advisory until ratified` clause. Expected residual: 17 lines, all PSC (13 pure + 4 mixed halves).

### R4.4 Census 2b — draft-to-draft cross-references (the seven draft files)

Actionable at C6 (drop draft/session framing on the six): CMM:266 (`the A0 sibling contract` → `the A0 contract`); IAC:146 (`the A2 draft` → `RESOURCE_OWNERSHIP_AND_LIFETIME.md`), IAC:239 (`session draft (A3/A8 proper)` → `(A3/A8)`), IAC:294 (`(A2 draft)` → drop), IAC:296 (`(session draft)` → drop); PSC:96, 138, 140, 177, 178 (`(draft, this session)` → drop — these reference TCM/ELT which ratify; PSC itself stays AUTHORED but its cites of ratified siblings must not call them drafts). KEEP: every reference to PSC as draft (IAC:70, 125, 277, 295; ROL:259). Draft self-labels in `title:`/H1 (`(A0 draft)` … `(A5+A6+A8 draft)`) flip inside each draft's own C6 ratification edit; PSC:13/20 keep theirs. Softer sibling-framing sites for operator wording at HALT-1: CMM:256 (`the A6 gap/contract` prose), ROL:113 (`the A5 contract's problem`), ROL:250-252 §7.3 heading + `Drafted in the same session`, ROL:263 footer (`Companion drafts from the same session … (A0–A8)`).

## R5 — Consolidated fix register

**Naming:** cluster-provenance fix ids (`EAM-n`, `ELT-n`, `ROL-n`, `CMM-n`, `TCM-n`, `IAC-n`, `PSC-n`, `MC-1/MC-2` for the mechanical classes) — the operator rules each id FIX-NOW / DEFER / REJECT at HALT-1. Every FIX-NOW candidate's exact replacement text is in this section or in R4; Phase B applies ratified text exactly. **Universal fix class (all six ratifying drafts + PSC): convert every `DOC:line` doc cite to CODING_STANDARDS §6.1 form** — `DOC §n (topic)` or bare stable id (`К-L18`, `PS-5`); line-pins stay only in code cites and dated records. The per-cite current locations are enumerated in the cluster tables (R1–R3) and the agents' verified location maps; the apply pass re-greps each target §-number at edit time.

### R5.1 — EAM fixes (C2 scope)

- **EAM-1 (R4-13 seed).** ANALYZER_RULES cites: `:186` "(ANALYZER_RULES.md §4)" for DFK003/DFK003_1 → `(ANALYZER_RULES.md §1.1 (shipped rule registry))`; `:187` and `:190` "…(ANALYZER_RULES.md line 269/271)" → `(ANALYZER_RULES.md §4 (deferred registry))`; `:200` "lines 269-272" → `§4 (deferred registry)`.
- **EAM-2.** Truth-pin re-verification texts (verbatim):
  - §2 header (:70): `De-facto owner (2026-07-15 truth)` → `De-facto owner (2026-07-15 truth; re-verified 2026-07-17 at HEAD 48983c4, DRAFTS_RATIFICATION)`.
  - Baseline (:33): → `Baseline for every "today" claim: working tree at HEAD 6f39903, 2026-07-15; re-verified at ratification HEAD 48983c4 (= 2309f9b) by the DRAFTS_RATIFICATION cascade, 2026-07-17 — code unchanged, doc anchors re-homed to the LOCKED successors per §6.1 form.`
  - Closing (:223): → `…every "today" claim was authored against HEAD 6f39903 and was re-verified against HEAD 48983c4 at ratification (DRAFTS_RATIFICATION, 2026-07-17): all code anchors held EXACT; all doc anchors were re-homed to the post-rework LOCKED successors.`
- **EAM-3.** Wholesale §6.1 re-homing (~40 anchors). Mapping (apply as listed; topics quoted):
  `KERNEL Part 0 line 60/61/62/64` → `KERNEL_ARCHITECTURE.md Part 0 К-L11 (NativeWorld SSoT) / К-L12 (native kernel scheduling) / К-L13 (wake model) / К-L15 (native bus authority)`; `KERNEL line 53` → `Part 0 К-L4 (explicit type-ID registry)`; `KERNEL line 547` → `§1.10 (component-type registry)`; `THREADING line 43/38/41/86/22` → `THREADING.md §3 (managed dispatch facade / dual registration — empty native access sets / facade retained at К10.1) · §6 (tick rates and wakes) · §2 (empty edge set)`; `EVENT_BUS line 17/76/77/47/49/123/59/78` → `EVENT_BUS.md §4 (division of labor today / BusFacade dormant / two native touchpoints) · §2 (deferred delivery; captured-context) · §7 (observability is inverted) · §6 (Background-tier capacity) [+ code cite per EAM-8] · §4 FENCED (planned pointer)`; `MOD_OS line 135/275/297` → `MOD_OS_ARCHITECTURE.md §0 (topological load order) / §2.2 (manifest dependencies field) / §2.3 (validation — Phases A–H)`; `ARCHITECTURE.md line 95/15/52` → `ARCHITECTURE.md §6 (where the truth lives) / §3 (scheduling story) / §2 (layer table) [+ EAM-6 annotation]`; `FRAMEWORK.md line 813/815` → `FRAMEWORK.md §14.7 (authority-surface predicate / tunable-law self-amendment clause)`; `PROJECT_AXIOMS.md line 132` → `PROJECT_AXIOMS.md §3.4 (three-graph + two-scheduler application)`; `K_CLOSURE_REPORT.md §2.17 (line 571) / line 75` → `K_CLOSURE_REPORT.md §2.17 (К-L15 canonical text) / §1.2 (К-L14 canonical text — "load-bearing authority surface")`; `SchedulerExtremeTests.cs:1007` → `:1009-1010`; KFNS cites → `historical/KERNEL_FULL_NATIVE_SCHEDULER.md (topic)`.
- **EAM-4.** §6.1 conflict-inventory refresh (verbatim resolved-notes):
  - Conflict 1 → `Resolved in-corpus: KERNEL_ARCHITECTURE.md §4 Rule 5 (reverse-invocation discipline) + Annex A-1 amend the predecessor's unidirectional Rule 5 to sanction the batched-callback reverse path; no outstanding conflict.`
  - Conflict 2 → `Resolved: KERNEL_ARCHITECTURE.md §6 (threading summary) retires the predecessor §1.4 "pool idle" text; the Part-4 "Native event bus … не planned" open question is gone (К-L15 LOCKED).`
  - Conflict 4 → `Resolved by retirement: the scheduling-liveness overstatement lived in KERNEL_FULL_NATIVE_SCHEDULER.md, now SUPERSEDED (historical/); SCHEDULER_ARCHITECTURE.md is the successor and THREADING.md §2 carries the "installed ≠ deciding" truth.`
  - Conflict 5 → `Resolved: KERNEL_ARCHITECTURE.md Part 0 К-L4 now scopes the FNV-1a prohibition to component ids and names the event-type id space as a separate, non-violating space owned by the bus.`
  - Conflict 3 (PA-003 §3.4) → KEEP as standing (PROJECT_AXIOMS unreworked; gated-tenure refinement owed via §5 protocol).
- **EAM-5.** Vacant-row rewording: R9 `ownerless in doc space — no document names lifecycle authority` → `no ratified (LOCKED) owner; the lifecycle/shutdown law is drafted in ENGINE_LIFECYCLE_AND_TRANSACTIONS.md + RESOURCE_OWNERSHIP_AND_LIFETIME.md (ratified this cascade)`; R11 `currently unwritten` → `drafted in IDENTITY_AND_ABI_CONTRACT.md (ratified this cascade); no LOCKED owner until its amendments land`; R12 `TBD — explicitly ownerless today` → `contract skeleton at PERSISTENCE_SNAPSHOT_CONTRACT.md (AUTHORED, held); an implementation owner must still be named before any save cascade` (wording final per HALT-1).
- **EAM-6.** P2 worked example (:175): append `(pre-rework example; the umbrella since clarified §1.4 — mods additionally reference the shared mods they depend on)` or re-anchor — operator choice at HALT-1.
- **EAM-7.** S10 anchor harmonization `:1007` → `:1009-1010` (2 sites: :149, :191).
- **EAM-8.** "schema v1" (:95, :215): cite code truth — `background_queue.cpp:209 (Item 31 wire format, К10.2 schema v1) + df_background_queue_serialize/deserialize (:242/:275)` — plus `EVENT_BUS.md §6 (Background-tier capacity)` and PSC as save-boundary owner.
- **EAM-9.** Preamble replacement (C6 payload; full text ratified at HALT-1):

> **Document class: LOCKED law (standalone).** Ratified authored-proposal → LOCKED v1.0.0 on 2026-07-17 by the DRAFTS_RATIFICATION cascade (EVT-2026-07-17-DRAFTS_RATIFICATION), per FRAMEWORK.md §7 (ratification protocol) / §7.2 (Tier-1 amendment-milestone protocol). This document is the A0 cross-cutting contract — the sole-authority map, the cutover gates and deletion triggers (§3), and the precedence proposal (§4). It joins the FRAMEWORK.md §14.7 (authority-surface predicate) surface as a Tier-1 LOCKED document.
>
> **Status of the cutover claims.** Every "today" claim was authored against HEAD `6f39903` (2026-07-15) and re-verified against ratification HEAD `48983c4` (`= 2309f9b`) by this cascade: all code anchors held EXACT; all cross-document anchors were re-homed to the post-rework LOCKED successors and now cite by stable section + topic per CODING_STANDARDS.md §6.1 (citation form). At ratification all eight cutover gates (§3 GATE-S1…S4, GATE-B1…B4) were confirmed OPEN.
>
> **Conflict rule (in force).** Where this document's §2 names the sole authority for a domain, that authority's document governs that domain (subject to §4, which is ADVISORY — see below). This document's own recorded conflicts with standing LOCKED law (§6.1) are an honest inventory to be closed by the named amendments, not a grant of precedence to this document over any LOCKED text; three of the originally-recorded conflicts were already resolved in-corpus by the 2026-07-15 KERNEL_ARCHITECTURE.md rework (§6.1 records their disposition).
>
> **Deferred fold path (forward amendment queue — NOT executed by this cascade).** The DRAFTS_RATIFICATION cascade ratifies this document **standalone**; it does **not** execute the section folds below. They are recorded as a forward amendment queue, each to be carried by its own amendment protocol when scheduled (tracked in docs/ROADMAP.md): §2 → ARCHITECTURE.md normative "who decides what" section (MINOR, additive); §3 → KERNEL_ARCHITECTURE.md Part 0 К-L12/К-L15 row annotations + chronicle entry; §4 → FRAMEWORK.md §14.7 amendment (schema-amendment protocol); §1/§5/§6 remain standalone. Until the §4 fold lands, **§4 P1–P3 remain ADVISORY** and conflicts between §14.7 surface members remain formally unarbitrable (§4 already states this — preserved unchanged). Until the §2/§3 folds land, ARCHITECTURE.md and KERNEL_ARCHITECTURE.md continue to cite this document as the cutover-gate authority (the citations upgrade from "(AUTHORED draft)" to LOCKED at ratification).

### R5.2 — ELT / ROL / CMM fixes (C3 scope)

- **ELT-1 (P1 class).** All ISOLATION / MOD_PIPELINE / KFNS / MIGRATION_PLAN / OWNERSHIP_TRANSITION cites gain the `historical/` prefix + verified offsets (ISOLATION `:70→:78`, `:65-73→:73-82`; MOD_PIPELINE `:319→:327`, `:33→:41`, `:228→:236`, `:339→:347`, `:340→:348`; OWNERSHIP_TRANSITION `:43→:51`) and reform to `historical/<doc> §(topic)` where prose allows. **R4-11 exact text** (:222): `Counter-example to eliminate: historical/OWNERSHIP_TRANSITION.md §"Transition table" closing rule (illegal transition throws InvalidOwnershipTransitionException in DEBUG, silently ignored in RELEASE with an error-counter increment). (Content relocated: the doc is superseded by DOC-J-GOLEM_OWNERSHIP; the pattern's canonical statement is preserved in the historical file.)`
- **ELT-2.** §1.1/§6-1 (:48-49, 277): → `MOD_OS_ARCHITECTURE §9.1 (commit/reclaim split — the predecessor's "transitions are atomic" and "no atomic-unload guarantee" sentences, reconciled). Note: this conflict is already resolved in the LOCKED successor, which adopts this draft's §1 law by name.`
- **ELT-3.** §2.3/§6-2 (:117-119, 128-132, 278): → `The three-way timing conflict is resolved in MOD_OS_ARCHITECTURE §9.5 (single code-verified fault path, was session finding N-8): code queues at fault time and runs the chain at next Apply; the MOD_PIPELINE and ISOLATION variants are retired to historical/. The detection gap (no per-system catch in ExecutePhase) remains open — ENGINE_LIFECYCLE §2.3 supplies its target design (MOD_OS §9.5 fences to this draft).` Doc-change list retargets: `ISOLATION.md:65-73/:70` → `historical/ISOLATION.md §"ModFaultHandler — lifecycle on a mod fault", step 4`; `MOD_OS:973` → `MOD_OS_ARCHITECTURE §10.3 (two response modes)`; `MOD_PIPELINE.md:228` → `historical/MOD_PIPELINE.md §"ModFaultHandler (unhandled mod exception)"`.
- **ELT-4.** §1/§6 corollary 3 (:61): `MOD_PIPELINE.md:319` → `MOD_OS_ARCHITECTURE §8.3 (graph-rebuild atomicity — the commit guarantee)`; MIGRATION_PLAN cites → `historical/MIGRATION_PLAN_KERNEL_TO_VANILLA.md §(atomic commit set / revert-as-unit)`.
- **ELT-5.** Quiescence §-fix (:38, :160): `MOD_OS §9.7` → `MOD_OS_ARCHITECTURE §9.6 (К-L18 quiescence — sim paused + pipeline slots quiescent)`. К-L cites (:38, :100, :200, :219) → bare `К-L18`/`К-L16`/`К-L19`.
- **ELT-6.** §6 conflict re-marks: 1, 2, 6 → RESOLVED (per ELT-2/ELT-3 + `MOD_OS §9.4 (one canonical chain)`); 3, 4, 5 → keep open, re-anchored (`historical/ISOLATION.md` step-4 abort wording; MOD_OS §9.1 fault-state absence; `MOD_OS §9.4 (step 3.5) + §9.6` for the KFNS-Item-41 tension). Preamble fold-target fix: "§4 fault taxonomy could fold into ISOLATION.md" → `MOD_OS_ARCHITECTURE §10 family (successor of the retired ISOLATION) or a standalone shutdown-law document`.
- **ELT-7.** Preamble replacement (C6 payload): > **Status: RATIFIED-IN-FORCE as a forward contract (EVT-2026-07-17-DRAFTS_RATIFICATION).** The A3+A8 transition/fault contract. Authored 2026-07-15 against HEAD `6f39903`; code-truth re-verified at HEAD `48983c4`, 2026-07-17 (src/native unchanged). **Fold status:** the §1 commit/reclaim law was already absorbed into MOD_OS_ARCHITECTURE §9.1 during the corpus rework (which cites this draft by name); the §2.3 fault-timing and §9.4 unload-chain reconciliations likewise landed in MOD_OS (was N-8/N-9). **Forward amendment queue** (folds NOT yet executed → recorded in ROADMAP): §3 named state machines (DesiredState/ReclamationState) → MOD_OS §9.1; §2.5 swapchain transaction → VULKAN_SUBSTRATE; §2.6 shutdown transaction + §4 fault taxonomy / Degraded → MOD_OS §10-family or a standalone shutdown-law doc. **Conflict carve-out:** of the §6 inventory, conflicts 1/2/6 are RESOLVED in the LOCKED successor; conflicts 3/4/5 remain open — until their named amendments land, the specific LOCKED texts (MOD_OS §9.4 best-effort semantics; §9.6 native quiesce) govern those points.
- **ROL-1.** `VULKAN_SUBSTRATE.md:1317` (×3 sites :30, :105, :244): → `VULKAN_SUBSTRATE §5.4 (vkDeviceWaitIdle policy — the LOCKED successor corrects the predecessor's phrasing: no save path exists yet, and swapchain recreation uses it in addition to shutdown; the process-wide shutdown gap is owned by this draft §4 / ENGINE_LIFECYCLE §2.6).`
- **ROL-2.** Quiescence §-fix (:106, :152, :220, :234): `MOD_OS §9.7` → `§9.6 (К-L18 quiescence)`.
- **ROL-3.** §6.1-form reforms: K-L8 cites → bare `К-L8`; FIELDS/EVENT_BUS/ECS cites → `FIELDS §(topic)` / `EVENT_BUS §(subscription lifecycle — "a missing Unsubscribe is a leak")` / `ECS §(span/batch protocol)`; MOD_OS chain cites → `§9.4`.
- **ROL-4.** §7.1-1 re-mark (:233): → `MOD_OS §9.1 already resolves the §9.1-vs-§9.5.1 contradiction (adopting this draft's logical/physical split); the fold of §5 no longer needs to resolve it — cite MOD_OS §9.1 as the resolved home (session finding C7, closed).` Items 2–5 keep (genuinely open).
- **ROL-5.** Preamble replacement (C6 payload): > **Status: RATIFIED-IN-FORCE as a forward contract (EVT-2026-07-17-DRAFTS_RATIFICATION).** The A2 ownership/lifetime/dispose contract. Authored 2026-07-15 against HEAD `6f39903`; code-truth (ownership tree, G1–G6 shutdown gaps) re-verified TRUE at HEAD `48983c4`, 2026-07-17. **Forward amendment queue** (folds recorded in ROADMAP, not yet executed): ownership tree §1 + dispose/destruction laws §4/§6 → a new LOCKED shutdown-law doc (or MOD_OS §10-family); §2 per-resource rows → ECS / FIELDS / MOD_OS §9 / VULKAN as additive amendments. **Conflict carve-out:** §7.1-1 is RESOLVED in the LOCKED successor (which adopts this draft's logical/physical split). §7.1 conflicts 2–5 remain open — until their amendments land, the named LOCKED texts govern (К-L18 mod-only scope; bus_native.h test-only df_bus_clear; KERNEL Part 7 void df_world_destroy; ECS/FIELDS lease-destruction silence).
- **CMM-1 (R4-8).** §4 edge 4 (:172): → `**Tick finalization → snapshot copy → next-tick read.** … established by the previous-tick snapshot mechanism defined normatively in THREADING §7 (feedback cycles and previous-tick snapshots) — "any read that closes a cycle must go through a _Previous snapshot of the previous tick". Gameplay applications: FEEDBACK_LOOPS (Category J, mechanics) §2/§6. ⚠ The establishing copy pass is unbuilt — §9.4.` §9.4 (:253): → `**Dangling snapshot anchor (re-homed).** The predecessor's "Phase 5 — Feedback snapshot" anchor is retired; THREADING §7 now owns the rule and confirms no *Snapshot component and no scheduler copy-pass exist (ParallelSystemScheduler.ExecuteTick, .cs:175-181). Edge §4.4 is normative-only until the copy pass is built or the rule re-anchored — THREADING §7 independently flags the same dangling state, citing this draft §9 item 4.`
- **CMM-2 (R4-9 — the honesty slip).** §8 analyzer row (:241): → `**Roslyn analyzer rules** — Today: the analyzer is live, not stubbed. DualFrontier.Analyzers ships **17 detecting rules** (9 Architecture / 5 NativeBoundary / 3 Discipline), enforced at Release-1.0 severities since А'.9.1 Phase β/γ (2026-07-01) — 11 Error + 5 Warning (build-breaking) + DFL025_B Info/IDE-only; waiver census pin = 2 (THREADING §8; ANALYZER_RULES §1.1 registry, §1.4 waiver census). **Gap:** none of the 17 covers async-context-affinity, span/batch escape, or missing-Dispose — those model elements stay unenforced (a coverage gap, not a non-detecting-stub state).`
- **CMM-3 (R4-12).** Change-history reconciliation — add rows (dates 2026-07-15): `0.1.3 | R4 mechanics corpus rework (0145f1b): see-also FEEDBACK_LOOPS → ../mechanics/ (Category J reclassification). Citation-only. PATCH.` · `0.1.2 | R2 platform corpus rework (6888246): see-also ISOLATION → ./historical/. Citation-only. PATCH.` · `0.1.1 | R1 kernel-core corpus rework (fe27fbe): see-also KERNEL_FULL_NATIVE_SCHEDULER → ./historical/. Citation-only. PATCH.` — **plus frontmatter bump 0.1.2 → 0.1.3** (recommended; matches register narrative). Alternative: fold R4 into the 0.1.2 row + correct the narrative. **Operator ruling at HALT-1.**
- **CMM-4.** §9.1 (:250) → `RESOLVED — KERNEL_ARCHITECTURE §1.4 no longer carries the pool-idle / single-threaded-writes claims: the LOCKED successor retired them as pre-К10.1 text (KERNEL §6) and delegates thread-safety to THREADING. §1-§2 here match the successor's truth; no live conflict remains.` §9.3 (:252) → `RESOLVED — the FIELDS successor §10 corrected "non-blocking" to "sync dispatch blocks until the fence signals"; §4.5 and the successor agree.`
- **CMM-5.** WRONG-cite retargets: `THREADING:95` → `THREADING §8 (analyzer enforcement)`; `VULKAN:1317` → `VULKAN §5.4 (waitIdle census)`; `FIELDS:372` → `FIELDS §10 (CPU/GPU exclusion — sync-dispatch fence semantics)`; body KFNS cites (:32, :251) → `historical/KERNEL_FULL_NATIVE_SCHEDULER.md Item 15/18 (topic)`; remaining THREADING/EVENT_BUS/KERNEL line cites → §6.1 form per the R2 location map.
- **CMM-6.** Preamble replacement (C6 payload): > **Document class: RATIFIED-IN-FORCE forward contract (EVT-2026-07-17-DRAFTS_RATIFICATION).** The A1 concurrency/memory model. Authored 2026-07-15 against HEAD `6f39903`; code-truth re-verified at HEAD `48983c4`, 2026-07-17 (src/native unchanged). No document may cite it as settled invariant text until the folds below execute; where a specific point still conflicts with a LOCKED doc, the carve-out governs. **Forward amendment queue** (ROADMAP-recorded; not executed): §3 matrix + §4 happens-before catalog → THREADING (the formal core; MAJOR) — §4 edge 4 re-homed to THREADING §7, already the rule's normative owner; §2 Vulkan rows + §6 fence/teardown → VULKAN_SUBSTRATE (MINOR); §1 census, §2 non-Vulkan owners, §5 lock order, §6–§9 → standalone here. **Conflict carve-out (re-verified):** §9.1 and §9.3 are RESOLVED — the LOCKED successors already retired/corrected those texts. §9.4's dangling snapshot anchor is re-homed to THREADING §7 (mechanism still unbuilt). Genuinely open — LOCKED texts govern until amended: §9.2 dual-executor (К-L12 cutover), §9.6 bounded-queue ownership, §9.7 C-ABI per-entry thread affinity (A6), §9.8 production native teardown (a build item).

### R5.3 — TCM / IAC / PSC fixes (C4 scope)

- **TCM-1 (R4-7).** §1 (:50): → `the fixed five-phase scaffold was retired in the THREADING rework; the managed phase count is data-dependent (THREADING §2 (native scheduler — BFS-layer phases) and §7 (feedback cycles — the correct anchor is the tick boundary, not a numbered phase)).` §1 (:52)/§1.1 (:96)/§7.5 (:253): → `the previous-tick snapshot copy happens at tick finalization (FEEDBACK_LOOPS §2 (snapshot-pattern shape)); the predecessor's numbered-phase anchor was retired with the five-phase scaffold — see historical/FEEDBACK_LOOPS.md for the retired wording.` §1.1 (:97)/§7.5: → `COMBO_RESOLUTION §2 (resolution shape); its [BridgeImplementation(Phase=5)] tag is original-content-roadmap phase numbering, not a scheduler PhaseId (COMBO_RESOLUTION §2 footnote).` §7.5 re-marked substantially closed.
- **TCM-2 (R4-10).** §4.3 (:177): → `production RNG is ad-hoc across four fixed-seed sites — MovementSystem.cs:35 (new(42)), GameBootstrap.cs:96 (ObstacleSeed), RandomPawnFactory.cs:58 and ItemFactory.cs:39 (constructor-injected seeds) — none world-derived, none persisted; no RNG service exists.` §5 D0 (:187): → `RNG is ad-hoc: four fixed-seed Random instances (canonical worst example MovementSystem.cs:35, new(42)), each with consumption order coupled to iteration/tick interleaving and position never persisted.` §6 (:235): `is the sole production instance today` → `is the canonical worst example of four fixed-seed instances today`. §6 rule 5 (:243) addition: `the analyzer ban must be semantic (type-based), not textual — a new System.Random regex misses target-typed new(42).`
- **TCM-3.** FIELDS non-blocking (:119, :175, :249): → `the predecessor FIELDS carried "field dispatches are non-blocking" (retired; historical/FIELDS.md); the FIELDS successor §10 (CPU/GPU exclusion — sync-dispatch fence semantics) now states "Sync dispatch blocks; it does not return early," matching rows 7/8 here.` §7.1 → closed row (named amendment already landed).
- **TCM-4.** §7 re-score: §7.1 CLOSED (FIELDS §10); §7.5 CLOSED (THREADING §7 tick-boundary); §7.2 reframed — `both LOCKED texts now defer to this contract (COMBO successor: "aspirational D2, effective D0" citing §5.2 C1–C6; VULKAN §7.2 cedes class vocabulary); the Option A/B decision is owned here, no LOCKED-vs-LOCKED contradiction remains`; §7.3 keep; §7.4 keep + `reconciliation now delegated to PSC §1 (PS-2) by VULKAN §7.1`. Remaining doc cites → §6.1 form per the R3 location map (THREADING §2/§3/§6/§7/§8; EVENT_BUS §2/§3/§4/§7; KERNEL К-L rows; VULKAN §2.4/§5.1/§5.4/§7.1/§7.2; FIELDS §4/§5/§10/§12; COMBO/FEEDBACK successor §§).
- **TCM-5.** Preamble replacement (C6 payload): > **Document class: RATIFIED — normative law in force** (was authored-proposal). Ratified AUTHORED → LOCKED under EVT-2026-07-17-DRAFTS_RATIFICATION. This is the A4 time/consistency contract; every architecture statement about time or visibility MUST be expressible in its §1 vocabulary and MUST cite a §5 determinism class. Anchors authored against HEAD `6f39903`; re-verified/retargeted to the LOCKED v1.0.0 successors at ratification HEAD `48983c4`. **Carve-out:** where a specifically-named LOCKED text still carries a conflicting sentence (§7 inventory), that text governs its specific point until the named forward amendment lands; §7.1 and §7.5 were resolved in place by the corpus rework and are recorded closed. **Forward amendment queue** (folds NOT executed; ROADMAP-recorded): THREADING absorbs §1–§3; KERNEL Part 0 / PERFORMANCE takes §5 classes; FIELDS §10 already carries the §7.1 correction (**DONE**); COMBO/VULKAN take the §5.2 Option A/B outcome.
- **IAC-1.** Census correction (§0 :41, §2 :82): `Ten production systems` / `13+ sites across 10 systems` → `nine production systems fabricate version-0 (≈20 sites), plus GameBootstrap.cs:241 and EntityEncoder.cs:85`; extend the site list with `ComfortAuraSystem.cs:61,:69` and `HaulSystem.cs:110,:144`; note the Version=1 sibling `WriteBatch.cs:221` alongside `SpanLease.cs:112` (aligns with ratified ECS §5).
- **IAC-2.** Teaching-texts re-mark (§0 :41, §2 :82, §7-2 :275): → `the pre-rework KERNEL §1.7 and ECS canonical example taught version-0 construction (retired; historical/KERNEL_ARCHITECTURE.md, historical/ECS.md); the ECS successor §4/§5 (fabricated-version defect) and KERNEL §1.7 now carry the corrected non-fabricating form and defer the identity fix to this contract's §2.` §7 item 2 → closed-by-rework.
- **IAC-3.** §7 item 1 (:274): → `the predecessor VULKAN §3.4 uint32_t field_id sketch was corrected to the shipped string-id ABI in the VULKAN successor §4.3 (field ABI); FIELDS §6 (field-id const char*) records the correction. Retired sketch: historical/VULKAN_SUBSTRATE.md.` → closed-by-rework.
- **IAC-4.** §3.4 (:154)/§7-6 (:279): → `KERNEL §5 (C ABI conventions — status-code + catch(...) immutable law; the successor records 72 catch(...) sites and forward-references this contract's §3–§4).` §4 (:191)/§7-7 (:283): → `device-lost has no mapping; the VULKAN successor now carries it as an open question (OQ-V1 / §6.3), still unresolved.`
- **IAC-5.** Remaining doc cites → §6.1 form (ECS §4/§6; EVENT_BUS §3/§4; FIELDS §4/§6/§8 — noting the field-grammar unification `<provider>.field.<verb>:<field-id>`; MOD_OS §9.8; ANALYZER §1; KERNEL §1.10/§5); KFNS cites already `historical/`-correct. `df_capi.h` header line-pins: full line-audit folds into the C4 apply pass (sampled EXACT).
- **IAC-6.** Preamble replacement (C6 payload): > **Document class: RATIFIED — normative law in force.** Ratified AUTHORED → LOCKED under EVT-2026-07-17-DRAFTS_RATIFICATION. Closes gaps A5 (identity) + A6 (C ABI protocol) + the A8 error-taxonomy portion; documents may now cite it as authority. Baseline anchors authored at HEAD `6f39903`; code re-verified unchanged at `48983c4`, doc anchors retargeted to the LOCKED v1.0.0 successors. **Carve-out:** named LOCKED texts govern their specific points until the §7 forward amendments land; §7 items 1 & 2 were resolved in place by the corpus rework and are recorded closed. **Forward amendment queue** (folds NOT executed; ROADMAP-recorded): §1 → KERNEL "Part 9: Identity registry" + per-doc amendments; §2 → ECS/KERNEL §1.7 + EntityId.cs/entity_id.h code change + DFK-entity-identity rule; §3–§5 → KERNEL Part 7/§5 expansion; §6 → ANALYZER additions + df_native_selftest extension.
- **PSC-1.** "schema v1" (fragment inventory :46, §2 :91, §5/PS-10 :131): → `the background-tier queue persists with the save file (KFNS Item 31, df_scheduler_serialize_background_queue, historical/KERNEL_FULL_NATIVE_SCHEDULER.md); EVENT_BUS §6 (background-tier capacity & save-format) is the live successor pointer.`
- **PSC-2.** Retargets: MOD_OS `§12 D-6 :1131 / §9.4 :916-920` → `MOD_OS §9.8 (save-game implications — D-6 strip/strict-toggle; major-version warn; lower-or-equal proceed)`; FIELDS `:296-305 family` → `FIELDS §12 (save/load — FENCED, defers to this contract) + §4 (forward-cites PS-7)`; VULKAN → `§7.1 (no drain at save; forward-refs this contract §1) / §7.2 (cedes determinism classes to TCM) / §5.4 (waitIdle census)`; FHE D7 `:96-100/:161` → `FHE_INTEGRATION_CONTRACT §D7 (versioning — ContractsVersion + reserved fourth field)`; `K_L3_1_AMENDMENT_PLAN:91` → `§(K-L3 reformulation — Path β runtime-only / Q4.b lock)`; remaining `DOC:line` → §6.1 form; historical/ cites stay.
- **PSC-3.** Refreshed verification note (inserted after the preamble block-quote; banner intact) + frontmatter `next_review_due: 'post-persistence-milestone closure'`: > **Anchor re-verification (EVT-2026-07-17-DRAFTS_RATIFICATION).** This skeleton is **held AUTHORED — not ratified**; the save-system milestone still owns implementation. Its anchors, authored against HEAD `6f39903`, were re-verified and retargeted at this cascade: all code cites (EntityEncoder.cs:68-85, SaveMeta.cs:17, GameBootstrap.cs:96, PipelineSlotInterop.cs:202, the DualFrontier.Persistence production-orphan) confirmed EXACT at HEAD `48983c4`; doc cites retargeted to the LOCKED v1.0.0 successors (notably EVENT_BUS §6 for the background-queue wire format — the predecessor's "schema v1" line was retired; MOD_OS §9.8 for D-6; FIELDS §12 and VULKAN §7, both of which now forward-reference this contract).

### R5.4 — Mechanical classes (C5 + C6 scope)

Exact per-site forms in **R4** (R4.2 census-1 replacements; R4.3 census-2a per-site table; R4.4 census-2b draft-to-draft sites). Class-1 flagged-for-wording sites: ANALYZER_RULES banner (3 predecessors + DF_TS retirement clause), MOD_OS banner (3-way merge rationale), FHE banner (prior-LOCK reconciliation) — proposed texts in R4.2, operator confirms wording. Class-2 mixed lines (4) edited per-token.

### R5.5 — Operator decisions beyond fix texts (HALT-1)

| ID | Question | Lean |
|---|---|---|
| OD-1 | Banner wording ×3 (ANALYZER retirement clause / MOD_OS merge rationale / FHE prior-LOCK) | accept R4.2 proposed texts (FHE: reconcile prior-LOCK v1.0 2026-05-06 history with the 2026-07-17 event) |
| OD-2 | Changelog status cells ×6 (ARCHITECTURE:158, CONTRACTS:159, ECS:179, THREADING:193, MOD_OS:727, ANALYZER:219+218) | leave as authored-session history; each doc gains a `1.0.0 — Ratified LOCKED per EVT-…` row at its own next PATCH (no touch this cascade) |
| OD-3 | CMM version reconciliation (R4-12) | **bump 0.1.2 → 0.1.3 + 3 change-history rows** (matches register narrative); alternative: fold into 0.1.2 + correct narrative |
| OD-4 | THREADING:193 changelog prose "A1/A2 AUTHORED drafts" | leave (descriptive history) or reword to "the A1/A2 contracts" |
| OD-5 | ROL §7.3 sibling-framing (:250-252 "Drafted in the same session…", :263 footer) + CMM:256 "A6 gap/contract" + ROL:113 "A5 contract's problem" | reword at C6 to post-ratification framing (five LOCKED siblings + A7 draft) — exact texts drafted at apply time from R4.4 list |
| OD-6 | EAM P2 worked example (EAM-6) | annotate "(pre-rework example; the umbrella since clarified §1.4)" |
| OD-7 | EAM vacant-row rewording final texts (EAM-5) | accept as drafted |
| OD-8 | **F-39 rider (C8)** — the two out-of-corpus "METHODOLOGY §7.1 determinism" pointers (`IDEAS_RESERVOIR.md`, `MAXIMUM_ENGINEERING_REFACTOR.md`) | ride (one commit, FHE-precedent re-anchor: contract-local commitment + TCM vocabulary — now citable as LOCKED law) or stay open |
| OD-9 | En-bloc ratification of the six (all verdicts READY-WITH-FIX / zero NOT-READY) + PSC hold confirmation | en bloc sanctioned by the brief when verdicts are clean |

### R5.6 — DEFER-to-F-ledger candidates (consolidated; → C7 ROADMAP seeding)

| Family | Members (evidence in R1–R3) | Queue destination |
|---|---|---|
| **Shutdown-law implementation** (ROL §4/G1–G6 + CMM §6–§7 + ELT §2.6/§3/§4) | ExecutePhase per-system catch + origin dispatch; UnloadAll first production caller; world-shutdown transaction (Join checked, deterministic NativeWorld.Dispose, teardown order); deferred-handler catch symmetry + regression test; df_world_active_span_count export + checked destroy; EngineSession adoption; swapchain prepare-before-reclaim; device-lost decision; Degraded surface; production native teardown (df_bus_clear promotion) | ROADMAP engineering queue (a) — the N-19 gap family, F-40 mod_unload-globals guard adjacent |
| **Identity versions surface** (IAC §2 + EAM Q4 + ECS §5 defect) | df_world_acquire_versions/release ABI; SpanLease versions view; DFK-entity-identity rule (semantic); IsValid → Index>0 both sides; ≈20-site atomic migration; EntityEncoder waiver until PS-5; ABA regression tests | ROADMAP engineering queue (b) |
| **GATE-S1..S4 / GATE-B1..B4 preparatory work** (EAM §3, all 8 OPEN) | access-set wiring; equivalence harnesses (scheduler lockstep, bus flag-on/off); dispatch switch; wake surface; BusFacade production construction; capability enforcement population; DomainEventBus counters | ROADMAP engineering queue (c) |
| **RNG service** (TCM §6, PS-14 precondition) | IRandomService.Stream(SystemId, SimTick); WorldSeed in save header; 4-site migration; semantic analyzer ban | new F-entry (queue (b)-adjacent or standalone) |
| **ABI protocol hardening** (IAC §3) | df_abi_version negotiation; thread-affinity declarations (DFK-abi-thread); DFK-abi-struct; native binary uncommitted + no CI | new F-entry |
| **Smaller records** | S10 anchor cosmetic; KFNS live-reference retargets; persistence-orphan disposition (PS §7.6); no-config-contract; input-bridge absent; native-Normal-tier dormant; TLA+ Item 18; bounded-queue owner | F-notes / existing F-35..F-38 wiring queue |

### R5.7 — HALT-1 decision checklist

1. **Per-draft ratification:** EAM / ELT / ROL / CMM / TCM / IAC — all READY-WITH-FIX, zero NOT-READY → en-bloc eligible (OD-9). PSC → held AUTHORED, retarget-only (confirmed).
2. **Fix list:** rule every R5.1–R5.3 id + MC-1/MC-2 (R4 forms) FIX-NOW / DEFER / REJECT.
3. **Both mechanical-class replacement forms** (R4.2 banner/checklist/Role/amendment forms; R4.3/R4.4 qualifier rules) — ratify, incl. OD-1 wording trio.
4. **Operator decisions OD-1..OD-9** (R5.5), incl. the F-39 rider (OD-8) and the CMM version ruling (OD-3).
5. **DEFER families** (R5.6) → confirm as the C7 ROADMAP forward-queue payload.

## R6 — Self-attestation

- Phase A filesystem output: **exactly one file — this report** (`docs/reports/DRAFTS_RATIFICATION_REVIEW_REPORT.md`). Zero edits to any other file in Phase A; zero git mutations in Phase A. (Phase 0 carried two recorded, operator-sanctioned git events on this branch: the branch creation and the H3 exclusion-rider commit `48983c4` — see R0.)
- All four Wave-R agents ran READ-ONLY and attested: zero writes, zero git mutations, `docs/architecture/historical/` treated as read-only reference (retarget proposals point AT it, never edit it). Orchestrator verification used read-only commands only; orchestrator spot-checks (enumerated in R1.3/R1.6, R2.3 flags, R3.2/R3.3) all CONFIRMED the agents' flagged items.
- Every census expression is recorded verbatim with its count (R0 baselines; R4 expressions); the class-1 12-vs-14 token nuance is recorded with both truths (R0, R4).
- Working tree at Phase-A close additionally carries only: the pre-existing untracked brief (enrolls at C1), the two operator template files excluded by the H3 rider (operator's files, untouched), and the machine-local `.claude/scheduled_tasks.lock` (never staged).
- Verdict totals: **six drafts READY-WITH-FIX, PSC RETARGET-NEEDED, zero NOT-READY**; ~200 anchor-verification verdicts across the seven drafts (code layer EXACT wholesale — the sole code drift is the two-line S10 test-attribute shift; doc layer systematically DRIFTED by the in-place rework, with the WRONG subset enumerated per cluster); 41 fix ids (R5.1–R5.3) + 2 mechanical classes (R4) + 9 operator decisions (R5.5) + 6 DEFER families (R5.6).
